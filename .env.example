# Copy this file to .env ONLY for local development (netlify dev).
# For production, set these in the Netlify dashboard: Site settings > Environment variables.
# Never commit real values. .env is ignored by Git.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=